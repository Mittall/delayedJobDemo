<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= best_in_place @user, :name %>
  <%#= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= best_in_place @user, :email%>
  <%#= @user.email %>
</p>


Text area : <%= best_in_place @user, :name, :as => :textarea, :ok_button => 'Save', :cancel_button => 'Cancel' %>

<%#= link_to 'Edit', edit_user_path(@user) %>
<%= link_to 'Back', users_path %>
